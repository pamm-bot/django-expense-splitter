# Screenshots

Referenced from the top-level `README.md`.

- **`group-detail.png`** — a group page after `seed_demo` (`/groups/<id>/`):
  the members list, the expense list, and the balances panel with its
  "Suggested" settlements.
- **`api-docs.png`** — the Swagger UI at `/api/docs/`, with one endpoint
  expanded.

Captured at ~1280px wide against the app's dark theme. To refresh: run the
server with `seed_demo` data, re-shoot both at the same width, and keep each
file well under ~500 KB.
