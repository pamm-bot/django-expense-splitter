# Screenshots

Referenced from the top-level `README.md`. Add two PNGs here:

- **`group-detail.png`** — a group page after `seed_demo`
  (`/groups/<id>/`): the members list, the expense list, and the balances
  panel with its "Suggested" settlements.
- **`api-docs.png`** — the Swagger UI at `/api/docs/`, expanded on an
  endpoint or two.

Capture at a normal browser width (~1280px), light or dark is fine as long
as both match. Keep each file well under ~500 KB.
